= form_for (@answer) do |f|
  = f.text_area :content, placeholder: 'Your Answer'
  = f.hidden_field :user_id, :value => current_user.id  
  = f.hidden_field :question_id, :value => @question.id
  = f.submit t('answers.btn.submit_answer')