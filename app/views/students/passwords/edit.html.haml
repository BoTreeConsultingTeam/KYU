.col-md-8.main-content
  = render :partial => "layouts/sub_heading", :locals => {:object => 'Change your password'}
  .modal-submit-questions
    .modal-dialog
      .modal-body
        = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f|
          = devise_error_messages!
          = f.hidden_field :reset_password_token
          %div
            = f.label :password, "New password"
            %br/
            = f.password_field :password, :autofocus => true, class: 'submit-input'
          %div
            = f.label :password_confirmation, "Confirm new password"
            %br/
            = f.password_field :password_confirmation, class: 'submit-input'
          %div= f.submit "Change my password", class: 'btn btn-primary'
        = render "students/shared/links"
